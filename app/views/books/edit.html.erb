<h1>Editing Book</h1>

<%= render 'layouts/error_messages' %>

<%= form_for @book do |f| %>

    <h4>Title</h4>
    <%= f.text_field :title %>

    <h4>Opinion</h4>
    <%= f.text_area :body %>

    <%= f.submit 'Update Book' %>

<%= link_to "Show", book_path(@book) %>
|
<%= link_to "Back", books_path %>
<% end %>